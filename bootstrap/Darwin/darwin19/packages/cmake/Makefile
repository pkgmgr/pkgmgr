PKGNAME=	cmake
PKGVER=		3.22.0

DISTFILE=	$(PKGNAME)-$(PKGVER).tar.gz
MASTERSITE=	https://cmake.org/files/LatestRelease

PKGROOT=	src/$(PKGNAME)-$(PKGVER)

USES=		make

CONFIG_TOOL=	sh:bootstrap
BUILD_TOOL=	make

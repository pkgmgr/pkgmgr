PKGNAME=	make
PKGVER=		4.3

DISTFILE=	$(PKGNAME)-$(PKGVER).tar.gz
MASTERSITE=	https://ftp.gnu.org/gnu/make

PKGROOT=	src/$(PKGNAME)-$(PKGVER)
SRCROOT=	$(PKGROOT)

DEPS=

CONFIG_TOOL=	configure
BUILD_TOOL=	make

#CONFIG_ARGS=	--prefix=<install_prefix>

INSTALL_TARGET=	install

BUILD_ARTIFACT=	bin/make
INST_ARTIFACT=	$(REL_PREFIX)/bin/make
